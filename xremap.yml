modmap:                                                                                                                                                                                                       
  - name: Default
    remap: # Required
      # Replace a key with another
      CapsLock: Backspace # Required  
keymap:
  - name: enter
    remap:
      Shift-Space: Enter
